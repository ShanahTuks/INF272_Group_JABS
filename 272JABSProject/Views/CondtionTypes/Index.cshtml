@model IEnumerable<_272JABSProject.Models.CondtionType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CondtionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CondtionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ConditionTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ConditionTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ConditionTypeID })
        </td>
    </tr>
}

</table>
